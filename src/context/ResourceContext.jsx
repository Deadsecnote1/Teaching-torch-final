/** @deprecated Import from `features/ol` — shim for backward compatibility */
export { ResourceProvider, useResources, normalizeResourceType } from '../features/ol/context/ResourceContext';
export { default } from '../features/ol/context/ResourceContext';
